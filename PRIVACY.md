# 隐私与数据存储 / Privacy

本说明适用于仓库发布的 Codex Pocket 1.3.1。Pocket 没有运营者云端数据库、广告 SDK 或自建分析上报服务。它连接用户填写的电脑桥地址；电脑里的 Codex 再按用户已有配置连接模型服务。模型供应商对请求、附件、日志的处理遵循其自身政策。

## 保存哪些数据

| 数据 | 位置与用途 | 清理方式 |
| --- | --- | --- |
| 桥地址和访问令牌 | Android 私有存储；AES-GCM 加密，密钥保存在 Android Keystore | 设置中「清除本机配对与离线记录」，或清除应用数据 |
| 主题 | App WebView 的本地存储 | 清除应用数据 |
| 当前会话、文字草稿、已读取消息、模型和项目选择 | Android 私有文件 `pocket-history.enc`；AES-GCM 加密，密钥留在 Android Keystore；不自动过期，单文件上限 64 MiB | 设置中「清除本机配对与离线记录」，或清除应用数据 |
| 未发送附件与预览 | 手机运行内存与临时缓存；不随离线聊天持久保存 | 关闭 App 或清除应用缓存 |
| 后台守护设置 | 电脑 `runtime/background.json`，只含 Node 路径与启用时间；当前用户启动目录快捷方式 | `Disable-Background.cmd` |
| 桥访问令牌 | 电脑 `runtime/pairing.json`；如设置环境变量则使用指定令牌 | 停止桥，移除配对文件或更换环境变量令牌后重启 |
| 上传的附件 | 电脑 `runtime/uploads/` | 确认任务已结束、不再引用文件后手动清理；不会自动过期 |
| 未选择已有项目时的工作文件 | 电脑 `runtime/workspaces/` | 按自己的项目备份和清理需要处理 |
| 启动及错误日志 | 电脑 `runtime/desktop.log`、`runtime/desktop-error.log` | 停止桥后清理；分享前先脱敏 |
| Codex 会话、项目路径、模型配置 | 用户原有的 Codex 数据与配置目录 | 通过电脑端相应方式管理 |

离线记录绑定已配对电脑的凭据摘要，不包含明文令牌，不保存待执行审批，也不会离线排队自动执行任务。Android 系统备份禁用，记录不上传 Pocket 云端；开发者浏览器预览仅使用会话存储。清除 App 缓存不等于清除私有聊天文件。

本项目不会把模型 API Key 打包到 APK。电脑上的 Codex 进程仍会读取并使用本机服务凭据；这不意味着模型请求不出网，也不保证敏感内容不会出现在模型输出或错误日志中。

## 谁能看到内容

作品预览只读取当前项目内在会话中明确引用的文件。Android HTML 预览使用没有 Capacitor 或原生 JavaScript 接口的独立 WebView，开发预览使用无同源权限的 iframe；配对令牌不传入内容，外网请求被禁止。HTML 在 Android 私有缓存中临时中转，独立预览读入后删除。分享时原文件保存到 Android 私有 `cache/artifacts/`，并通过系统选择器授权接收应用读取所选文件；超过一天的旧分享缓存会在下次分享时清理，也可清除应用缓存。

可选后台启动创建当前 Windows 用户的启动快捷方式与本地守护设置，后台检查桥是否退出并重新启动；不保存系统登录密码，不自动打开聊天窗口。撤销后守护会在下一次检查结束，当前桥继续运行。它不能替代系统锁屏对现场人员的隐私保护。

可选异地配置向导使用 Tailscale 外部服务；登录身份、设备及网络连接元数据由该服务按其政策处理。向导不会另存 Tailscale 密码、认证链接、配对码或桥令牌。会添加仅作用于电脑 Tailscale 网卡/IP、TCP 15731 和 Tailscale 来源网段的防火墙规则，可用 `Remove-Remote.cmd` 移除；撤销该规则不等于撤销手机凭据或其他已有网络权限。

- 手机、运行桥的电脑、获得桥令牌的客户端，可在权限范围内访问会话与执行能力。
- 接受任务的模型服务可能收到提示词、文件内容和工具结果。
- 默认 HTTP 连接没有加密，所在网络的其他参与者可能截获内容或凭据。请使用可信网络或私人 VPN。
- 同一电脑桥没有独立用户隔离，不适合作为多人共用的公网服务。

## 断开、撤销与卸载

手机「清除本机配对与离线记录」移除这部手机的本地凭据和已保存聊天，**不会撤销电脑端已有令牌**。电脑配对页「生成新配对码」也不会使已经配对的设备失效。需要撤销时，先停止桥进程，再移除 `runtime/pairing.json` 并重新启动；若使用 `POCKET_BRIDGE_TOKEN`，则更换该变量。所有旧设备都要重新配对。

卸载手机 App 不会删除电脑上的会话、日志或附件。项目代码没有设置附件自动清理策略。

## 公开发布范围

仓库和电脑桥下载包仅包含源码、文档、界面资源及发布所需文件。个人运行目录、上传文件、服务配置、签名私钥、浏览器配置、开发日志和真实对话截图不属于发布内容。展示图片使用空白状态或内置演示。

提交 Issue、录屏和日志时，请隐藏服务密钥、令牌、配对码、私人路径、文件内容及会话标识。
