# 更新记录

## 1.3.2-bridge.1 — 异地向导登录等待修复

- 连接命令超时后重新读取 Tailscale 的实际状态，避免用户完成授权后仍停在失败结果。
- 区分未登录、等待设备审批、连接关闭和本机离线；登录期间轮询，长时间等待后可按 Enter 继续或 R 重连。
- 打开本机认证链接并提示确认设备授权；不把网页设备列表视为已连接。
- 增加只读 `-StatusOnly` 排障入口，扩写从安装、两端登录到移动数据确认的详细指南。
- 修复 Windows PowerShell 5.1 管理员窗口错误解码 UTF-8 中文账号名称导致 JSON 解析失败；提升权限步骤失败时回传具体错误。
- APK 保持 1.3.2，无需重复安装。
- Windows 电脑完成配置与专用规则检查，用户确认手机移动数据下可同步；校园网尚未实测。

## 1.3.2 — 等待状态与会话释放

- 手机展示后台任务阶段、等待时长和独立的「停止本轮」按钮；离线状态明确提示进度待同步。
- 模型连接重试、执行工具、等待审批和停止过程有明确提示，不显示结构化思考正文。
- 后台持有会话时，旧桌面快照不再覆盖真实运行状态；历史列表同步也保留后台活跃状态。
- 本轮结束后释放失败会重试；已落盘会话启动失败时也释放，保留新建空会话的安全处理。
- 通过持久化轮次状态恢复遗漏的完成通知，避免已完成任务一直显示运行中。
- Windows 锁屏不等于会话锁；没有添加 Windows 密码采集或自动解锁功能。模型服务响应速度仍取决于用户所用服务。

## 1.3.1 — 自动连接与离线记录

- Android 将已读取聊天、当前会话、文字草稿和模型选择加密写入私有文件，关闭 App 或断网后仍可打开。
- 复用已保存配对，启动、回到前台和网络恢复时自动同步；连接失败保留聊天与草稿，不自动重发命令。
- 已归档与普通历史同时保留本地副本；后台暂未返回完整内容时不清空已有记录。
- Windows 登录后运行后台守护，桥或引擎退出后自动重新启动；保存 Node 路径，减少登录环境差异。
- `Enable-Background.cmd` 升级/启用守护，`Disable-Background.cmd` 撤销；不绕过 Windows 登录，电脑仍需保持唤醒，跨网依赖私人 VPN。
- APK 1.3.1，versionCode 7；未在用户手机上进行断网重启与跨网络实测。

## 1.3.0-bridge.1 — 异地连接配置向导

- 体验改进交付后，增加 `Setup-Remote.cmd`：检测/引导安装 Tailscale、登录、启动后台桥并配置专用访问规则。
- 配对页自动选中 Tailscale 地址，减少误填局域网地址。
- `Remove-Remote.cmd` 可移除本向导添加的规则；保留账号、其他网络和启动配置。
- 手机仍使用 1.3.0 APK。首次需要安装 Tailscale、本人登录和系统权限，不保证全部校园网可达。
- 未进行用户校园网与跨网手机的端到端实测。

## 1.3.0 — 会话与后台体验

- 修复新会话首条消息前过早释放写入者，导致记录文件缺失及恢复失败。
- 移除自动弹出桌面会话；无桌面所有者时由后台引擎处理，完成后释放。
- 手机归档、查看已归档会话及恢复。
- 隐藏结构化思考内容。
- 提供项目作品列表、图片/HTML/文本/媒体预览及原文件分享。
- 可选 Windows 登录后后台启动，以及撤销脚本。
- 旧版缺失文件会话可「带入新对话」，保留待发送内容。
- 防火墙工具仅作可选网络排障，不作为兼容性要求。

后台活跃任务需本轮完成后再由桌面接管；HTML 预览禁止外网，复杂模块或服务端功能需在电脑运行。本版未新增异地联网配置。

## 1.2.1-bridge.1 — 电脑桥防火墙工具

- 新增 `Fix-Firewall.cmd` / `.ps1`，为电脑桥配置 TCP 15731 的专用网络、本地子网入站规则。
- 优先定位正在监听的桥进程，避免为错误的 Node.js 路径放行。
- 提示热点的公用网络分类，以及优先级高于允许规则的 Node.js 阻止规则；支持移除本工具的放行规则。
- 补充防火墙排障说明。Android App 仍为 1.2.1，无需重新安装。

## 1.2.1 — 首个公开预览版

- Android App 与 Windows 电脑桥，沿用本机 Codex / cc-switch 服务配置。
- 首页对话、侧边记录搜索、底部设置入口、四种主题和 Android 图标。
- 同一条桌面对话中发送手机消息，同步状态、审批与停止请求。
- 从电脑读取可用模型、推理强度和已有项目。
- 附件上传，每条最多 6 个、每个 20 MB；常见图片转为图片输入。
- 键盘避让、按会话保留草稿、失败保留输入、复制回复。
- 修正模型选择弹层及桌面占用提示反复刷新的问题。
- 六位配对码与 Android Keystore 凭据存储。
- 添加中英文介绍、安装与开发指南、隐私和安全说明。

发布说明：APK 使用 debug 签名；同会话发送依赖 Codex 桌面内部 IPC。Android 最低版本为 8，Windows 是当前交付平台，不承诺全部桌面版本、手机或远程网络环境兼容。
